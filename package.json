{
  "name": "namethattune",
  "version": "1.0.0",
  "description": "Dudle",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "gcp-build": "cd client && npm install && npm run build",
    "deploy": "gcloud app deploy --project=dudle-488408"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
